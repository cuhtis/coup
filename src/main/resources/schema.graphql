type Query {
    actions: [Action!]!
    cards: [Card!]!
    standard_deck: Deck!
}

type Action {
    name: String!
    description: String!
}

type Card {
    name: String!
}

type Deck {
    card_count: Int!
}
